pipeline {
  agent none
  stages {
    stage ( "Build" ) {
    agent any
    options {
      skipDefaultCheckout()
       }
       stage {
       echo 'hello world'
       }
       }
       }
       }
